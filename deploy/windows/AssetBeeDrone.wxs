<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs"
     xmlns:ui="http://wixtoolset.org/schemas/v4/wxs/ui"
     xmlns:util="http://wixtoolset.org/schemas/v4/wxs/util">

	<Package
		Name="AssetBee Drone"
		Manufacturer="AssetBee"
		Version="$(var.Version)"
		UpgradeCode="A7E3C2B1-4D5F-6A78-9B0C-1D2E3F4A5B6C"
		Scope="perMachine"
		Language="1033">

		<!-- ARP metadata helps enterprise allowlists and reduces "unknown publisher" noise. -->
		<Property
			Id="ARPCOMMENTS"
			Value="AssetBee device inventory agent." />
		<Property
			Id="ARPCONTACT"
			Value="AssetBee" />

		<Upgrade Id="A7E3C2B1-4D5F-6A78-9B0C-1D2E3F4A5B6C">

			<UpgradeVersion
				Property="WIX_UPGRADE_DETECTED"
				Minimum="0.0.0"
				Maximum="$(var.Version)"
				IncludeMinimum="yes"
				IncludeMaximum="no" />

			<!--
				Version 1.0.0 required ENDPOINT even while RemoveExistingProducts was
				uninstalling it. Detect that release separately so the full UI can
				remove it before the new install transaction begins.
			-->
			<UpgradeVersion
				Property="LEGACY_UPGRADE_DETECTED"
				Minimum="1.0.0"
				Maximum="1.0.0"
				IncludeMinimum="yes"
				IncludeMaximum="yes"
				OnlyDetect="yes" />

			<UpgradeVersion
				Property="SAME_VERSION_PRODUCT"
				Minimum="$(var.Version)"
				Maximum="$(var.Version)"
				IncludeMinimum="yes"
				IncludeMaximum="yes" />

			<UpgradeVersion
				Property="WIX_DOWNGRADE_DETECTED"
				Minimum="$(var.Version)"
				IncludeMinimum="no"
				OnlyDetect="yes" />

		</Upgrade>

		<!-- Only prevent actual downgrades. -->
		<Launch
			Condition="NOT WIX_DOWNGRADE_DETECTED"
			Message="A newer version of AssetBee Drone is already installed." />

		<MediaTemplate EmbedCab="yes" />

		<WixVariable
			Id="WixUILicenseRtf"
			Value="$(var.SourceDir)\License.rtf" />

		<!-- Runtime configuration -->
		<Property Id="ENDPOINT" Secure="yes" />
		<Property Id="BEARERTOKEN" Secure="yes" Hidden="yes" />
		<Property Id="APIKEY" Secure="yes" Hidden="yes" />
		<Property Id="UNINSTALL_PRODUCTS" Secure="yes" Hidden="yes" />
		<!-- /qn or /qb for legacy/same-version uninstall via msiexec -->
		<Property Id="UNINSTALL_MSIUI" Secure="yes" Hidden="yes" Value="/qn" />

		<!-- Existing configuration -->
		<Property Id="BT_EXISTING" Secure="yes" Hidden="yes" />
		<Property Id="AK_EXISTING" Secure="yes" Hidden="yes" />

		<Property Id="EP_HKLM" Secure="yes">
			<RegistrySearch
				Id="ExistingEndpointHklm"
				Root="HKLM"
				Key="SOFTWARE\AssetBee\Drone"
				Name="Endpoint"
				Type="raw" />
		</Property>

		<Property Id="BT_HKLM" Secure="yes" Hidden="yes">
			<RegistrySearch
				Id="ExistingBearerHklm"
				Root="HKLM"
				Key="SOFTWARE\AssetBee\Drone"
				Name="BearerToken"
				Type="raw" />
		</Property>

		<Property Id="AK_HKLM" Secure="yes" Hidden="yes">
			<RegistrySearch
				Id="ExistingApiKeyHklm"
				Root="HKLM"
				Key="SOFTWARE\AssetBee\Drone"
				Name="ApiKey"
				Type="raw" />
		</Property>

		<Property Id="EP_HKCU" Secure="yes">
			<RegistrySearch
				Id="ExistingEndpointHkcu"
				Root="HKCU"
				Key="SOFTWARE\AssetBee\Drone"
				Name="Endpoint"
				Type="raw" />
		</Property>

		<Property Id="BT_HKCU" Secure="yes" Hidden="yes">
			<RegistrySearch
				Id="ExistingBearerHkcu"
				Root="HKCU"
				Key="SOFTWARE\AssetBee\Drone"
				Name="BearerToken"
				Type="raw" />
		</Property>

		<Property Id="AK_HKCU" Secure="yes" Hidden="yes">
			<RegistrySearch
				Id="ExistingApiKeyHkcu"
				Root="HKCU"
				Key="SOFTWARE\AssetBee\Drone"
				Name="ApiKey"
				Type="raw" />
		</Property>

		<Feature
			Id="Main"
			Title="AssetBee Drone"
			Level="1">

			<ComponentGroupRef Id="ProductComponents" />

		</Feature>

		<StandardDirectory Id="ProgramFiles64Folder">

			<Directory
				Id="AssetBeeFolder"
				Name="AssetBee">

				<Directory
					Id="INSTALLFOLDER"
					Name="Drone" />

			</Directory>

		</StandardDirectory>

		<ComponentGroup
			Id="ProductComponents"
			Directory="INSTALLFOLDER">

			<Component
				Id="InstallFolderAcl"
				Guid="A1B2C3D4-E5F6-7890-ABCD-EF1234567890">

				<CreateFolder>
					<util:PermissionEx
						User="SYSTEM"
						GenericAll="yes" />

					<util:PermissionEx
						User="Administrators"
						GenericAll="yes" />

					<util:PermissionEx
						User="Users"
						GenericRead="yes"
						GenericExecute="yes" />
				</CreateFolder>

				<RemoveFile
					Id="RemoveAppSettings"
					Name="appsettings.json"
					On="uninstall" />

				<RemoveRegistryKey
					Root="HKLM"
					Key="SOFTWARE\AssetBee\Drone"
					Action="removeOnUninstall" />

				<RemoveRegistryKey
					Root="HKCU"
					Key="SOFTWARE\AssetBee\Drone"
					Action="removeOnUninstall" />

			</Component>

			<Component
				Id="DroneBinary"
				Guid="B1C2D3E4-F5A6-7890-1234-56789ABCDEF0">

				<File
					Id="DroneExe"
					Source="$(var.PublishDir)\AssetBee.Drone.exe"
					KeyPath="yes" />

				<ServiceInstall
					Id="AssetBeeDroneService"
					Name="AssetBeeDrone"
					DisplayName="AssetBee Drone"
					Description="Collects and securely reports device inventory."
					Type="ownProcess"
					Start="auto"
					Account="LocalSystem"
					ErrorControl="normal"
					Vital="yes" />

				<ServiceControl
					Id="StartAssetBeeDroneService"
					Name="AssetBeeDrone"
					Start="install"
					Stop="both"
					Remove="uninstall"
					Wait="yes" />

				<util:ServiceConfig
					ServiceName="AssetBeeDrone"
					FirstFailureActionType="restart"
					SecondFailureActionType="restart"
					ThirdFailureActionType="restart"
					RestartServiceDelayInSeconds="15"
					ResetPeriodInDays="1" />

			</Component>

			<Component
				Id="TrayBinary"
				Guid="E4F5A6B7-C8D9-0123-4567-89ABCDEF0123">

				<File
					Id="TrayExe"
					Source="$(var.PublishDir)\AssetBee.Drone.Tray.exe"
					KeyPath="yes" />

				<RegistryValue
					Root="HKLM"
					Key="Software\Microsoft\Windows\CurrentVersion\Run"
					Name="AssetBeeDroneTray"
					Type="string"
					Value="&quot;[#TrayExe]&quot;" />

			</Component>

			<!--
				Installed helper (proper path/name). Never use Binary table EXE CAs:
				MSI extracts those to %TEMP%\*.tmp which AV routinely blocks as W32/Exploit.gen.
			-->
			<Component
				Id="MsiHelperBinary"
				Guid="F5A6B7C8-D9E0-1234-5678-9ABCDEF01234">

				<File
					Id="MsiHelperExe"
					Source="$(var.PublishDir)\AssetBee.Drone.MsiHelper.exe"
					KeyPath="yes" />

			</Component>

		</ComponentGroup>

		<UIRef Id="AssetBeeDroneUI" />

		<!--
			Legacy / same-version uninstall via system msiexec (no temp-extracted helper).
			UNINSTALL_PRODUCTS is a product code; UNINSTALL_MSIUI is /qn or /qb.
		-->
		<CustomAction
			Id="UninstallRelatedProducts"
			Directory="System64Folder"
			ExeCommand="msiexec.exe /x [UNINSTALL_PRODUCTS] [UNINSTALL_MSIUI] /norestart ENDPOINT=&quot;[ENDPOINT]&quot;"
			Execute="immediate"
			Impersonate="no"
			Return="check" />

		<!-- Copy AppSearch values into the primary MSI properties -->

		<CustomAction
			Id="SetEndpointFromHklm"
			Property="ENDPOINT"
			Value="[EP_HKLM]" />

		<CustomAction
			Id="SetEndpointFromHkcu"
			Property="ENDPOINT"
			Value="[EP_HKCU]" />

		<CustomAction
			Id="SetBearerExistingFromHklm"
			Property="BT_EXISTING"
			Value="[BT_HKLM]" />

		<CustomAction
			Id="SetBearerExistingFromHkcu"
			Property="BT_EXISTING"
			Value="[BT_HKCU]" />

		<CustomAction
			Id="SetApiKeyExistingFromHklm"
			Property="AK_EXISTING"
			Value="[AK_HKLM]" />

		<CustomAction
			Id="SetApiKeyExistingFromHkcu"
			Property="AK_EXISTING"
			Value="[AK_HKCU]" />

		<CustomAction
			Id="RestoreBearerOnly"
			Property="BEARERTOKEN"
			Value="[BT_EXISTING]" />

		<CustomAction
			Id="RestoreApiKeyOnly"
			Property="APIKEY"
			Value="[AK_EXISTING]" />

		<CustomAction
			Id="RestoreBearerPrefer"
			Property="BEARERTOKEN"
			Value="[BT_EXISTING]" />

		<!-- Runs from Program Files after InstallFiles — never from %TEMP%. -->
		<CustomAction
			Id="WriteAppSettings"
			FileRef="MsiHelperExe"
			ExeCommand="write-settings --endpoint=&quot;[ENDPOINT]&quot; --bearer=&quot;[BEARERTOKEN]&quot; --api=&quot;[APIKEY]&quot; --install=&quot;[INSTALLFOLDER].&quot;"
			Execute="deferred"
			Impersonate="no"
			HideTarget="yes"
			Return="check" />

		<CustomAction
			Id="LaunchTray"
			FileRef="TrayExe"
			ExeCommand=""
			Execute="immediate"
			Impersonate="yes"
			Return="asyncNoWait" />

		<InstallUISequence>

			<Custom
				Action="SetEndpointFromHklm"
				After="AppSearch"
				Condition="NOT ENDPOINT AND EP_HKLM" />

			<Custom
				Action="SetEndpointFromHkcu"
				After="SetEndpointFromHklm"
				Condition="NOT ENDPOINT AND EP_HKCU" />

			<Custom
				Action="SetBearerExistingFromHklm"
				After="AppSearch"
				Condition="NOT BT_EXISTING AND BT_HKLM" />

			<Custom
				Action="SetBearerExistingFromHkcu"
				After="SetBearerExistingFromHklm"
				Condition="NOT BT_EXISTING AND BT_HKCU" />

			<Custom
				Action="SetApiKeyExistingFromHklm"
				After="AppSearch"
				Condition="NOT AK_EXISTING AND AK_HKLM" />

			<Custom
				Action="SetApiKeyExistingFromHkcu"
				After="SetApiKeyExistingFromHklm"
				Condition="NOT AK_EXISTING AND AK_HKCU" />

			<Custom
				Action="RestoreBearerOnly"
				After="SetApiKeyExistingFromHkcu"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND BT_EXISTING AND NOT AK_EXISTING AND NOT REMOVE" />

			<Custom
				Action="RestoreApiKeyOnly"
				After="RestoreBearerOnly"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND AK_EXISTING AND NOT REMOVE" />

			<Custom
				Action="RestoreBearerPrefer"
				After="RestoreApiKeyOnly"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND BT_EXISTING AND NOT REMOVE" />

		</InstallUISequence>

		<InstallExecuteSequence>

			<Custom
				Action="SetEndpointFromHklm"
				After="AppSearch"
				Condition="NOT ENDPOINT AND EP_HKLM" />

			<Custom
				Action="SetEndpointFromHkcu"
				After="SetEndpointFromHklm"
				Condition="NOT ENDPOINT AND EP_HKCU" />

			<Custom
				Action="SetBearerExistingFromHklm"
				After="AppSearch"
				Condition="NOT BT_EXISTING AND BT_HKLM" />

			<Custom
				Action="SetBearerExistingFromHkcu"
				After="SetBearerExistingFromHklm"
				Condition="NOT BT_EXISTING AND BT_HKCU" />

			<Custom
				Action="SetApiKeyExistingFromHklm"
				After="AppSearch"
				Condition="NOT AK_EXISTING AND AK_HKLM" />

			<Custom
				Action="SetApiKeyExistingFromHkcu"
				After="SetApiKeyExistingFromHklm"
				Condition="NOT AK_EXISTING AND AK_HKCU" />

			<Custom
				Action="RestoreBearerOnly"
				After="SetApiKeyExistingFromHkcu"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND BT_EXISTING AND NOT AK_EXISTING AND NOT REMOVE" />

			<Custom
				Action="RestoreApiKeyOnly"
				After="RestoreBearerOnly"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND AK_EXISTING AND NOT REMOVE" />

			<Custom
				Action="RestoreBearerPrefer"
				After="RestoreApiKeyOnly"
				Condition="NOT BEARERTOKEN AND NOT APIKEY AND BT_EXISTING AND NOT REMOVE" />

			<!--
				AppSearch already captured endpoint/secrets into session properties before
				RemoveExistingProducts; no temp helper / preserve script is required.
			-->
			<RemoveExistingProducts
				After="InstallInitialize" />

			<Custom
				Action="WriteAppSettings"
				After="InstallFiles"
				Condition="NOT REMOVE" />

			<Custom
				Action="LaunchTray"
				After="InstallFinalize"
				Condition="NOT REMOVE" />

		</InstallExecuteSequence>

	</Package>

</Wix>
